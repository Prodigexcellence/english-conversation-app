# Copie ce fichier en `.env` puis remplis les deux valeurs.
# Elles se trouvent dans Supabase → Project Settings → API.
#
# ⚠️ Ne mets JAMAIS la clé API Claude ici : elle vit uniquement dans les secrets
# de l'Edge Function (`supabase secrets set ANTHROPIC_API_KEY=...`).

EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
